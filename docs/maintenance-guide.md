# TCS Maintenance Guide

## Backups

- Use `scripts/backup-db.sh` to create backups of the database.

## Migrations

- Apply migrations using `scripts/migrate-db.sh`.

## Seeding

- Seed the database using `scripts/seed-db.sh`.
